/*
SortOrder takes an array of objects, a key to be sorted on 
and a key for the return value. 
*/
// // function sortOrder(data, sortKey, value) {
// //     const { sortKey, value } = data;
// //     console.log(sortKey)

// }

//Notes from the class, this is the solution -- 

// const sortOrder = (data, sortKey, name) => {
// 	// let arr = data.map(values => {
// 	//     const {sortOrder , name } = values.sort()
// 	// })
// 	// return arr.sort();

// 	arr.sort((a, b) => {
// 		return a;
// 	});
// };

// const sortOrder = (arr, sort,key) => {
//     arr.sort((a,b) => {
//         if()
//         return a[sorted] - b[sorted];
//     }).map(element => return {
//         element[key]
//     })
// }

//when we use the sort function, we use compartive, compare the first letter of the 1st index of word

module.exports = { sortOrder };
